namespace: longhorn-system
helmCharts:
  - namespace: longhorn-system
    repo: https://charts.longhorn.io
    name: longhorn
    releaseName: longhorn
    version: 1.4.1
    includeCRDs: true

resources:
  - namespace.yml
  - ingress.yml